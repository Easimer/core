#include runtime.cor

fn pure fib(n : real) : real {
	if(n ? 0.0) then return(0.0);
	if(n ? 1.0) then return(1.0);
	return(fib(n - 2.0) + fib(n - 1.0));
}

fn Main() : bool {
	convtest : real = 1;
	print(fib(10.0));
	return(true);
}

